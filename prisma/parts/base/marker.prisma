import { Comment } from "./comment"

model Marker {
  id       Int      @id @default(autoincrement())
  datetime Int
  lat      Float?
  lng      Float?
  alt      Float?
  comment  Comment?
  imageId  Int?
}
