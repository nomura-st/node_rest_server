generator client {
  provider = "prisma-client-js"
  output   = "./generated/db_append"
}

datasource db_append {
  provider = "sqlite"
  url      = "file:./dev_append.db"
}

// Models
model Image {
  id        Int  @id @default(autoincrement())
  url       String?
}
